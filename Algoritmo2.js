const numbers = [1,2,3,4,5]; //declara uma constante chamada numbers e a inicializa com um array contendo os valores 
const output = arr.map((x)=>{
  return x * 2;  
}); //declara a chamada oupt e tenta atribuir ao resultado e existe um erro na variavel arr e se excutar havera erro 
console.log(numbers); //imprime o valor da variavel
